# Raw Syslog corpus

These files are retained only as neutral immutable input examples for the
future Custom parser. The collector runtime does not parse them and no current
test treats the old category/RADIUS/AntiFraud expectations as authoritative.

Files with historical names are preserved byte-for-byte so later parser work
can compare against the captured source corpus.
